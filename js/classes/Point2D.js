/**
 * Represents a 2D point.
 */
class Point2D {
    /**
     * @param {number} x X coordinate.
     * @param {number} y Y coordinate.
     */
    constructor(x, y) {
        this.x = x;
        this.y = y;
    }
}